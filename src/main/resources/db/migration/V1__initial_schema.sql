-- JOB_TITLE
CREATE TABLE JOB_TITLE (
    ID_JOB_TITLE NUMBER NOT NULL AUTO_INCREMENT,
    NAME_VALUE VARCHAR2(50) NOT NULL
);

ALTER TABLE JOB_TITLE
ADD CONSTRAINT PK_JOB_TITLE PRIMARY KEY (ID_JOB_TITLE);

-- COMPANY
CREATE TABLE COMPANY (
    ID_COMPANY NUMBER NOT NULL AUTO_INCREMENT,
    NAME_VALUE VARCHAR2(50) NOT NULL
);

ALTER TABLE COMPANY
ADD CONSTRAINT PK_COMPANY PRIMARY KEY (ID_COMPANY);

-- TECHNOLOGY
CREATE TABLE TECHNOLOGY (
    ID_TECHNOLOGY NUMBER NOT NULL AUTO_INCREMENT,
    NAME_VALUE VARCHAR2(50) NOT NULL
);

ALTER TABLE TECHNOLOGY
ADD CONSTRAINT PK_TECHNOLOGY PRIMARY KEY (ID_TECHNOLOGY);

-- WORK_EXPERIENCE
CREATE TABLE WORK_EXPERIENCE (
    ID_WORK_EXPERIENCE RAW(16) NOT NULL,
    CREATED_AT TIMESTAMP NOT NULL,
    USER_ID VARCHAR2(50) NOT NULL,
    BINDING BOOLEAN NOT NULL,
    ID_JOB_TITLE NUMBER NOT NULL,
    VISIBILITY_JOB_TITLE BOOLEAN NOT NULL,
    ID_COMPANY NUMBER NOT NULL,
    VISIBILITY_COMPANY BOOLEAN NOT NULL,
    VISIBILITY_TECHNOLOGIES BOOLEAN NOT NULL,
    START_DATE DATE NOT NULL,
    END_DATE DATE,
    VISIBILITY_WORK_PERIOD BOOLEAN NOT NULL,
    SALARY NUMBER(10,2) NOT NULL,
    CURRENCY VARCHAR2(3) NOT NULL,
    VISIBILITY_SALARY BOOLEAN NOT NULL
);

ALTER TABLE WORK_EXPERIENCE
ADD CONSTRAINT PK_WORK_EXPERIENCE PRIMARY KEY (ID_WORK_EXPERIENCE);

ALTER TABLE WORK_EXPERIENCE
ADD CONSTRAINT FK_WE_JT
FOREIGN KEY (ID_JOB_TITLE) REFERENCES JOB_TITLE(ID_JOB_TITLE);

ALTER TABLE WORK_EXPERIENCE
ADD CONSTRAINT FK_WE_C
FOREIGN KEY (ID_COMPANY) REFERENCES COMPANY(ID_COMPANY);

-- WORK_EXPERIENCE_TECHNOLOGY
CREATE TABLE WORK_EXPERIENCE_TECHNOLOGY (
    ID_WORK_EXPERIENCE RAW(16) NOT NULL,
    ID_TECHNOLOGY NUMBER NOT NULL
);

ALTER TABLE WORK_EXPERIENCE_TECHNOLOGY
ADD CONSTRAINT FK_WET_WE
FOREIGN KEY (ID_WORK_EXPERIENCE) REFERENCES WORK_EXPERIENCE(ID_WORK_EXPERIENCE);

ALTER TABLE WORK_EXPERIENCE_TECHNOLOGY
ADD CONSTRAINT FK_WET_T
FOREIGN KEY (ID_TECHNOLOGY) REFERENCES TECHNOLOGY(ID_TECHNOLOGY);